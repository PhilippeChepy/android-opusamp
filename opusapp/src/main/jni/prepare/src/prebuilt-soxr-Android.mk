LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_MODULE := soxr

LOCAL_SRC_FILES := $(LOCAL_PATH)/$(TARGET_ARCH_ABI)/libs/libsoxr.so
LOCAL_EXPORT_C_INCLUDES := $(LOCAL_PATH)/include
LOCAL_EXPORT_LDLIBS := $(LOCAL_PATH)/$(TARGET_ARCH_ABI)/libs/libsoxr.so

include $(PREBUILT_SHARED_LIBRARY)